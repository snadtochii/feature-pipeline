.DS_Store

# Pipeline artifacts and tickets are local working state, not for the plugin repo
claudedocs/

# Node tooling for the tidy-loop checks fixtures
node_modules/
coverage/
*.tsbuildinfo

# Bytecode, should a local probe import a script under scripts/ rather than run it
__pycache__/
