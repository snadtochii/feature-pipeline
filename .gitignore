.DS_Store

# Pipeline artifacts and tickets are local working state, not for the plugin repo
claudedocs/

# Node tooling for the tidy-loop checks fixtures
node_modules/
coverage/
*.tsbuildinfo

# Bytecode left behind when a script under scripts/ is imported rather than run
__pycache__/
